server:
  port: 8888
spring:
  application:
    name: configserver
  cloud:
    config:
     server:
       git:
         uri: https://github.com/Cybermatepro/iw-env-config
         default-label: main
